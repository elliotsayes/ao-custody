{
  "name": "test-teal",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "bash ./scripts/build.sh",
    "clean": "rimraf ./build-lua ./build",
    "deploy": "WALLET_JSON=\"$(cat ./.secret/wallet.json)\" aoform apply",
    "generate-wallet": "npx -yes @permaweb/wallet > ./.secret/wallet.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "aoform": "^1.0.4",
    "rimraf": "^6.0.1"
  }
}
